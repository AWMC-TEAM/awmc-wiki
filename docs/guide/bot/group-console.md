# 群管理后台（群管理面板）

::: tip 一句话
「群管理后台」是 AWMC Bot 的网页控制台。群主 / 管理员在浏览器里就能开关本群功能、
查看群数据、管理机厅排卡与天气，不用再记一大堆文字指令喵～
:::

## 一、怎么进入

在目标群里向 Bot 发送：

```text
群管理
```

别名：`管理后台`、`群后台`。

进入需要满足以下任一条件：

- 你是本群的**群主或管理员**（角色由平台自动识别，普通群成员无法进入）；
- 你已被超级管理员在群后台中**授权**为本群管理；
- 你是插件超级管理员。

默认情况下，还需要先完成 [论坛账号绑定（qbind）](/guide/bot/intro#八、论坛账号绑定qbind)，
后台会使用论坛身份确认「打开链接的是你本人」。

发送 `群管理` 后，Bot 会返回一条**一次性登录链接**（5 分钟内有效，只能用一次）：

- 默认模式：链接带 `#invite=...`，打开后按页面引导完成论坛授权，再自动跳回后台；
- 快速登录（服务器开启时）：链接带 `#fast=...`，打开即可直接登录，无需再授权。

::: danger 登录链接会出现在群里
Bot 回复的链接可能被群内其他成员看到。请打开后尽快使用，不要转发给他人；快速登录
模式下链接可以直接登录，更要小心保管喵～
:::

如果 Bot 提示无法确认管理权限，请发送：

```text
后台身份
```

把返回的身份 ID 交给超级管理员，由管理员在群后台中为你授权。

## 二、后台能做什么

| 模块 | 说明 |
|------|------|
| 群总览 | 查看你名下各群的今日活跃、消息量与最近活跃时间 |
| 功能开关 | 一键开启 / 关闭本群的成绩查询、猜歌、BREAK、排卡、账号、搜索、游玩次数等功能 |
| 群数据统计 | 查看近 1 / 7 / 30 / 90 天的 BREAK 收支、功能调用占比、成员消息排行与使用流水 |
| 机厅排卡 | 添加、编辑、删除机厅；从 Nearcade 自动补全店铺资料；查看上报历史 |
| 天气 | 开启每日天气摘要推送，设置推送时间、恶劣天气提醒与订阅周期 |
| 消息模板 | 自定义排卡的查询 / 上报 / 预测文案（仅超级管理员） |
| 成员授权 | 超级管理员可把某个 `后台身份` 授权为本群管理，授权默认 30 天有效 |

后台中的关键操作都会记录审计日志，方便管理员回溯。

## 三、常见问题

| 提示 | 解决办法 |
|------|----------|
| 「群管理后台尚未启用」 | 服务器未开启该功能，请联系 Bot 管理员部署开启 |
| 「请先发送 qbind 完成论坛绑定」 | 先发送 `qbind` 完成论坛账号绑定 |
| 「未能确认群管理权限」 | 发送 `后台身份`，把返回的身份 ID 交给超级管理员授权 |
| 「请先在群管理设置机厅经纬度」 | 进入后台，在「机厅排卡」中添加机厅并填写经纬度 |
| 链接打不开或报错 | 确认是从 Bot 刚返回的新链接打开；链接 5 分钟且仅一次有效，过期请重新发送 `群管理` |

## 四、给部署者的配置说明

群管理后台是独立监听的 Web 服务，默认端口 `8100`，必须通过 HTTPS 反向代理对外提供。

```text
# 是否启用群管理后台
MAIMAIDX_GROUP_CONSOLE_ENABLED=true
# 对外 HTTPS 地址，必须以 /group-console 结尾
MAIMAIDX_GROUP_CONSOLE_PUBLIC_URL=https://console.example.com/group-console
# 监听地址：仅允许 127.0.0.1 / ::1 / localhost，或显式 0.0.0.0（必须经反向代理）
MAIMAIDX_GROUP_CONSOLE_HOST=127.0.0.1
MAIMAIDX_GROUP_CONSOLE_PORT=8100
# 快速登录（一次性链接直接登录）；默认关闭，群内链接可被其他成员看到
MAIMAIDX_GROUP_CONSOLE_FAST_LOGIN=false
# 超级管理员紧急登录 Token；日常建议使用论坛 UID/QQ + OAuth
MAIMAIDX_GROUP_CONSOLE_ADMIN_TOKEN=
# 群后台专用论坛 OAuth 回调地址；留空则使用 {PUBLIC_URL}/api/login/callback
MAIMAIDX_GROUP_CONSOLE_OAUTH_REDIRECT=
```

其它相关配置：

- `MAIMAIDX_QUEUE_NEARCADE_TOKEN`：Nearcade 机厅搜索 / 同步 Token；
- `MAIMAIDX_QUEUE_WEATHER_PROVIDER`：天气数据源（`openmeteo` / `qweather` / `both`）；
- `MAIMAIDX_QUEUE_WEATHER_QWEATHER_KEY` 等：和风天气 JWT 相关配置。

群后台的数据保存在 `data/group_console/` 下，排卡与天气数据在
`data/group_console/queue.db`，不会写入事件循环热路径。

相关入口：[指令帮助](/guide/bot/advanced#212-高级功能) · [使用说明](/guide/bot/intro) ·
[赞助支持](/sponsor)
